//- Para dar el titulo de la pagina usar                 -var titulo = "titulo de la pagina"

//- para usar geogebra usar el mixin en el header            +usarGeogebra
//- para comenzar una nueva diapositiva usar el mixin        +diapositiva("titulo de la diapositiva")
//- 	el bloque hijo sera incluido como parte de la diapositiva
//-			Usar para añadir el contenedor de contenido					 .diapo__contenido
//-			Para agregar una Nota, comentario, extra usar				 .comentario
//- 	es recomendable incluir imagenes,tables en un bloque figure 
//-		con una descripcion figcaption
//- 		para agregar una imagen usar el mixin                 +imagen(archivo, descripcion)
//- 	  para incluir un archivo de geogebra usar el mixin     +graficoGeogebra(nombreArchivo,comandos='',width=400,height=400)
//-	      el archivo debe incluirse en la carpeta geogebra/
//- 	    En comandos se puede pasar javascript y comandos de Geogebra mediante api.comando_geogebra
//-		    Ejemplo: "api.setCoordSystem(-1,7,-2,6)"
//- se agrega al final la navegacion con el mixion			      +navegacion

extend pug/_plantilla.pug
prepend head
	- var titulo = "Pagina creada por Mario Estanislao Cesar Ariet"
	//- Poner una descripcion del tema de la pagina. Sobre todo para cuando arme una pagina como indice general.
	meta(name="description", content="descripción del tema")
	//- A continuación poner los tag que describen los temas de las diapositvas
	//- también incluir para quienes esta dirigida la diapositiva, materia, codigo materia, cbc
	meta(name="keywords", content="palabras claves")

	+usarGeogebra

block body

	+diapositiva("Cónicas")
		.diapo__contenido
			h3 Expresión general
			p Se llama cónica al lugar geometrico formado por todos los puntos $(x,y)$ que cumplen con una ecuación de segunda grado como la siguiente:
			p \[ \alpha x^2 +\beta xy + \gamma y^2 + \lambda x + \mu y + \nu = 0\]
			.comentario
				h3 Nota
				ul
					li Al menos un término cuadratico ($\alpha x^2, \beta xy \text{ o bien } \gamma y^2$ ) debe ser distinto de cero. 
					li Cuando $\beta = 0$ la cónica se encuentra alineada con el eje horizontal o el eje vertical.

			h3 Ejemplos
			ul
				li $x^2+4y+3=0$ (parábola)
				li $3x^2+y^2-5=0$ (elipse)
				li $(x-2)^2-(y+2)^2=1$ (hipérbola)
				li $xy=4$ (hipérbola)
		figure
			+graficoGeogebra("conicas","api.setCoordSystem(-10,10,-10,10)")

	+diapositiva("Parábola")
		.diapo__contenido
			div
				h3 Horizontal
				p \[ (y-k)^2-4c(x-h)=0\]
					ul
						li Vertice $(h,k)$
						li Foco $(h+c,k)$
						li Directriz $ x=h-c$
			div
				h3 Vertical
				p \[ (x-h)^2-4c(y-k)=0\]
					ul
						li Vertice $(h,k)$
						li Foco $(h,k+c)$
						li Directriz $ y=k-c$
		figure
			+graficoGeogebra("parabola","api.setCoordSystem(-15,15,-15,15)")
			figcaption Es el lugar geométrico de los puntos $P$ que se encuentra a la misma distancia de un punto $F$ (foco) y una recta $L$ (directriz). \[ \frac{d(P,F)}{d(P,L)}=1\]

	+diapositiva("Elipse")
		div
			h3 Horizontal 
			p \[ \frac{(x-h)^2}{a^2}+\frac{(y-k)^2}{b^2}=1\]
				ul
					li Vertices $(h\pm a,k)$
					li Focos $(h\pm c,k)$
					li Directrices $ x=h\pm \frac{a^2}{c}$
		div
			h3 Vertical 
			p \[ \frac{(x-h)^2}{b^2}+\frac{(y-k)^2}{a^2}=1\]
				ul
					li Vertices $(h,k\pm a)$
					li Focos $(h,k\pm c)$
					li Directrices $ y=k\pm \frac{a^2}{c}$	
		figure
			+graficoGeogebra("parabola","api.setCoordSystem(-10,10,-10,10)",600,600)
			figcaption Es el lugar geométrico de los puntos $P$ que se encuentra a la misma distancia de un punto $F$ (foco) y una recta $L$ (directriz). \[ \frac{d(P,F)}{d(P,L)}=1\]



		
			

block header
	+navegacion
